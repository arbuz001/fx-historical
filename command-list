# to add changes to the code you did to the repository
git add .
git commit -m "explain why you add this change"
# git remote add origin https://github.com/arbuz001/fx-historical.git
git push -u origin master

# to update your local copy and get all the latest chages from the repositry
git pull origin master

# to see the log of changes
git log

# to see all the differences
git diff --author="arbuz001"

# to hard revert all the local differences
git checkout path/to/revert
git checkout -- .

# get autocompletion in python
import rlcompleter
import readline
readline.parse_and_bind("tab: complete")

# get current working directory
import os
cwd = os.getcwd()


# add directories to import modules from
import sys
from pprint import pprint

pprint(sys.path)
sys.path.insert(0, "/home/alex/Documents/kaggle/data-fx-historical/code/")
pprint(sys.path)
